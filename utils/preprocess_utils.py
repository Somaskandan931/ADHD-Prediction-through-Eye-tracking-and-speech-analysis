# preprocess_utils.py

def clean_input(raw_input: dict) -> dict:
    """
    Optional: normalize or validate input dictionary.
    """
    # Add defaults or clip ranges if needed
    processed = raw_input.copy()
    return processed
